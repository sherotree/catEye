<!--index.wxml-->
<view class='container'>
  <view class='title row'>
    <label>影片</label>
    <label>总票房(万)</label>
    <label>票房占比</label>
  </view>
  <view class='content row' wx:for="{{movies}}" data-detail="{{item}}" wx:key="{{item.movieId}}">
    <label>{{item.movieName}}</label>
    <label>{{item.boxInfo}}</label>
    <label>{{item.boxRate}}</label>
  </view>
</view>